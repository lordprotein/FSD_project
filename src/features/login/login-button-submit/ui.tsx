import { Button } from 'shared/ui/button'

export const LoginButtonSubmit = () => (
  // eslint-disable-next-line no-alert
  <Button title="Log In" onClick={() => alert('Log In Button clicked')} />
)
